# コマンドプロンプト
# フロントエンドとバックエンドをバックアップ
# 場所：/var/www/html/backup
# 手動で、バックアップ完了後にパスワードマネージャー（パスワード管理サービス）に保管する

# 1) バックエンドをバックアップ（/opt/tshare-api）
sudo tar -czpf "/var/www/html/backup/backend_tshare-api_$(date +%Y%m%d_%H%M%S).tar.gz" /opt/tshare-api

# 2) フロントをバックアップ（/var/www/html）※ backup/ を除外して自分自身問題を回避
sudo tar -czpf "/var/www/html/backup/frontend_html_$(date +%Y%m%d_%H%M%S).tar.gz" \
  --exclude="/var/www/html/backup" \
  /var/www/html

# 3) 作成確認
sudo ls -lh /var/www/html/backup
